schema_version = "1.0.0"
postmortem_id = "beta"
title = "Equivalence gap in cutover"
owner = "standard"
failure_mode = "equivalence-gap"
severity = "medium"
summary = "The slice was functionally close but lacked proof that both paths matched."
evidence = [
  "shadow run diverged on one edge case",
]
actions = [
  "add an equivalence gate before cutover",
]
notes = [
  "kept advisory because the blocker was not production-breaking",
]
source = "docs/incidents/beta.md"
