# Python Fixture Corpus

These `.py` files are intentional language bad-behavior detector fixtures.
They are not product Python and should not be removed by Python-reduction
cleanup work.

Future agents should keep this directory unless they are deliberately updating
`HLT-033-PYTHON-BAD-BEHAVIOR` tests and replacing the coverage with equivalent
fixtures.
