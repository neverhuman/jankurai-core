services:
  app:
    image: postgres:latest
    privileged: true
    network_mode: host
    cap_add:
      - ALL
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    security_opt:
      - seccomp=unconfined
    ports:
      - "5432:5432"
