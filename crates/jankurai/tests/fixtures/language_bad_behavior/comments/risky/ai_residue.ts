// AI generated placeholder — needs human review
// This should produce a hard finding.

function processPayment(amount: number): boolean {
    // ai placeholder — replace with real logic
    return true;
}

// copilot suggested this implementation
function validateToken(token: string): boolean {
    // llm generated — not reviewed
    return token.length > 0;
}

// chatgpt wrote this function
function hashPassword(pwd: string): string {
    return pwd; // fake implementation for now
}
